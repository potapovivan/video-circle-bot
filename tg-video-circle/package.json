{
  "name": "video-circle-bot",
  "version": "1.0.0",
  "description": "Telegram bot that converts videos to round video notes (circles) with sound.",
  "scripts": {
    "build": "bash install_ffmpeg.sh",
    "start": "bash install_ffmpeg.sh && node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "fluent-ffmpeg": "^2.1.2",
    "fs-extra": "^11.2.0",
    "telegraf": "^4.15.3"
  }
}
